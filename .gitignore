/.venv/
# Unanchored (unlike the root entry above) so it also covers device_mcp_catalog/.venv/ and
# any other subproject's own venv.
.venv/
/logs/
/.pytest_cache/
/.context/
/__pycache__/
*.pyc
*.pyo
*.pyd
*.swp
*.swo
*.tmp
*.log
coverage.xml
.coverage
htmlcov/
dist/
build/
*.egg-info/
ENV/
env/
venv/
venv.bak/
pip-log.txt
pip-delete-this-directory.txt

# Build / tool caches
.mypy_cache/
.pytest_cache/

# IDE and local tooling
.claude/

# Local agent automation (kept out of the repo)
/autonomy/
/AGENTS.md

# Local-only deployment overrides — machine-specific, never committed
docker-compose.lite.local.yml
unifi-lite-local-config.yaml

# Runtime data — generated files, not source
/devices.db
/data/

#temporary files
3rd*.txt